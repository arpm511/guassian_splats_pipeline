# Gaussian Splatting Pipeline Dependencies
# Python 3.11+ required

# Core dependencies
numpy>=1.24.0,<2.0.0
opencv-python>=4.8.0
pillow>=10.0.0
tqdm>=4.66.0
plyfile>=1.0.0
matplotlib>=3.8.0

# COLMAP Python bindings (CUDA-enabled version)
# Use pycolmap-cuda12 for CUDA 12.x systems with GPU
pycolmap>=0.6.0
# pycolmap-cuda12>=0.6.0  # Uncomment for CUDA-enabled wheels

# Deep learning (PyTorch)
# Install separately with: uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch>=2.1.0
# torchvision>=0.16.0

# Utilities
pyyaml>=6.0
pathlib  # Included in Python 3.11+ stdlib, here for compatibility
